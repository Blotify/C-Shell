#ifndef KEYBOARD_H
#define KEYBOARD_H
#include <stdio.h>
#include <stdlib.h>
#include <signal.h>
#include <unistd.h>
extern pid_t shell_pid;
void hsi(int);void hst(int);void hsq(int);void skh();void sfp(pid_t);
#endif
